# T3 CLOSURE — shared one-time product fulfillment — WSTERA-HOUSE-PRODUCTION-CLOSURE-001

Stage: T3 (shared one-time product fulfillment)
Review Batch: **B3 — Fulfillment Durability Boundary**
Task: `WSTERA-HOUSE-PRODUCTION-CLOSURE-001`
Recorded: 2026-09-20 (Asia/Bangkok)
Orchestrator: Hermes

## Delivered

Repository: `Gutumrod/hub-web`
Branch: `work/house-platform-closure-20260919`
**Source revision: `ae29d202c7ee60a579e85209ec6575fdd357c060`** (pushed; parity verified)
Base before T3: `e24d5a91742f0c841fea395f01e864e5fe8fa1a5`

```
17 files changed, 5762 insertions(+)
```

| File | Lines | Role |
|---|---|---|
| `drizzle/schema.ts` | +318 | fulfillment tables + unique indexes (**additive only**) |
| `drizzle/migrations/0007_shared_one_time_fulfillment.sql` | 272 | creates them |
| `server/fulfillment/repository.ts` | 897 | durable persistence, attempts, audit, revoke/reissue state |
| `server/fulfillment/service.ts` | 1050 | grant/deliver/revoke/reissue, visible durable failure |
| `server/fulfillment/fulfillment.test.ts` | 1283 | eight acceptance proofs |
| `server/fulfillment/fulfillment-controls.test.ts` | 587 | control + mutation-probe suite |
| `server/fulfillment/{types,idempotency,db}.ts` | 355 | contract types + key/digest derivation |
| `server/fulfillment/vendor/**` (8 files) | 1000 | destination-owned copies + `PROVENANCE.md` |

## Orchestrator-verified gates (run by Hermes, not taken from a worker)

```
cd apps/hub-web
npx tsc --noEmit  -> exit 0
npx vitest run    -> 21 files, 272 tests, all passing     (pre-T3 baseline: 213)
secret scan       -> 22 files, 0 findings (canonical Relay scanner)
git diff --name-only -- server/webhooks/  -> empty (untouched)
```

**Gate reading caveat (recorded in `GATE-COVERAGE-FINDING-TEST-TYPECHECK-2026-09-20.md`):**
`tsconfig.json` excludes `**/*.test.ts`, so `tsc --noEmit` verifies **production source only**.
`vitest run` is the binding evidence for test behaviour. Every `tsc` claim in this task's evidence
carries that scope.

## Locked contract compliance

| Requirement (`:503-507`, `:894` R13, `:368` L4) | Implemented |
|---|---|
| accept an already-authoritative entitlement input without becoming payment truth | service consumes an `AuthoritativeEntitlementInput`; no provider SDK, no billing table, no payment-mutation name in executable code |
| bind recipient to an immutable artifact/repository version | recipient binding + `immutableVersion` on the grant; delivery recorded against it |
| deliver/grant idempotently under repeat and retry | two distinct controls: `(grantKey, generation)` suppresses an identical replay; the `bindingKey` partial index refuses a second live grant |
| fail visibly and durably rather than silently | failed handoff leaves durable FAILED state on grant + attempt and returns the error; a payload-less delivery records the version linkage without moving bytes |
| audit who received which immutable version | audit entries carry both `immutableVersion` and `recipientDigest` (proof 7) |
| support revoke | proof 5, including repeat-revoke suppression |
| support reissue | proof 6, `revocation` releases the binding so reissue is possible |
| never package WSTERA production secrets | secret scan 0 findings; fixtures use obvious sentinels |
| product-specific artifact contents outside House scope | synthetic artifact only |
| built once and shared | single `server/fulfillment/` capability, not per-product |

Eight acceptance proofs are present as named suites at
`fulfillment.test.ts:645, 697, 763, 852, 891, 950, 1079, 1175`.

## Reuse Gate compliance

`T3-REUSE-GATE-2026-09-20.md` = PASS. Copy-and-own performed and recorded in
`server/fulfillment/vendor/PROVENANCE.md`: source version, immutable commit
`cd88c570ab57f6976d15f85d09973d0cfbf0cd63`, copy date and local changes for
`audit-log` 0.1.0, `file-storage` 0.1.0, `job-retry` 0.3.0. No cross-repository import.
`modules/subscription` deliberately **not** adopted (billing-truth engine).

## Adjudication applied

`T3-SCHEMA-MIGRATION-DRIFT-ADJUDICATION-2026-09-20.md` — the schema/migration divergence on the
live-grant invariant was adjudicated (migration moved to `bindingKey` + renamed
`fulfillment_records_live_binding_unique`) and the four schema-omitted unique indexes were declared.
Applied in the delivered revision; the control suite pins both facts.

## Failure history for this stage (all recorded, none hidden)

| # | Fingerprint / defect | Outcome |
|---|---|---|
| 1 | `T3_IMPLEMENTATION_TURN_BUDGET_EXHAUSTED_BEFORE_CORE_SERVICE` | schema + migration + vendor + provenance landed and survived |
| 2 | six typecheck errors (incl. the TS2802 iterator class seen twice earlier in this task) | fixed; `tsc` exit 0 |
| 3 | schema/migration drift (raised by the worker, adjudicated by Hermes) | applied |
| 4 | `recipientDigest` out of scope in `runHandoff` → 20 failures | fixed by threading it from both call sites |
| 5 | imprecise control assertions (raw-substring vs documentation comments) | fixed via comment-stripping that keeps string literals |
| 6 | undefined test helper `withoutComments` | fixed by exporting/importing one shared definition |

Stage used six bounded lanes (`t3-wu02` → `wu02a` → `wu02b` → `wu02c` → `wu02d` → `wu02e`), each
keeping prior progress. That lane count is itself a finding about worker envelope sizing for
capability builds — recorded for planning, not as a blocker.

## Transition

T3 delivered -> **B3 independent review required** before T4.

Reviewer independence for B3: implementing worker was `swarm-builder`; `agent-codex` reviews. Bind
the review to hub-web revision `ae29d20` (the source lives in a git-ignored nested repository).
