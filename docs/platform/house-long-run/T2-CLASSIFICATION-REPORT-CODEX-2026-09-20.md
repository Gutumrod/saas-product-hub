VERDICT: WORKER_FIX
FINGERPRINT ASSESSMENT: correct
CONTRADICTION ASSESSMENT: real and unsatisfiable — `productEvents.ts:53` places `bodyDigest` inside the body; `:294-295` compares it to `sha256(rawBody)`. `canonicalSigningPayload` also includes that field at `:115-130`.
AUTHORIZED REMEDY: B — remove `bodyDigest` from the JSON body; compute `sha256(rawBody)` server-side and include that computed digest in the canonical HMAC material. This satisfies the Master Plan requirement that the signed envelope includes version, key ID, product ID, event ID, issued-at time, and body digest (`PORTFOLIO_PRODUCTION_MASTER_PLAN.md:192-194`) without self-reference. It preserves tamper resistance because the digest remains HMAC-bound, while retaining the existing `X-Hub-Signature` transport-header pattern (`productEvents.ts:29-30`).
DECISION CLASS: bounded technical — the plan locks the required signed fields and per-product binding (`PORTFOLIO_PRODUCTION_MASTER_PLAN.md:192-194, 884, 918-919`) but does not lock whether the digest is serialized inside the body, transported separately, or derived from raw bytes. This is therefore authorized for Hermes dispatch, not an OWNER_DECISION_REQUIRED gap.
REMEDY SHAPE FOR NEXT LANE: In `apps/hub-web/server/webhooks/productEvents.ts`, remove `bodyDigest` from `productEventSchema`; compute the raw-body SHA-256; change `canonicalSigningPayload` to accept the computed digest separately; HMAC the canonical fields plus computed digest with the registered signer secret; keep `X-Hub-Signature`, server-side `keyId -> productId` binding, replay, idempotency, size, and rate checks. `signerRegistry.ts` should remain unchanged unless a narrowly scoped helper is needed. Rewrite/extend `productEvents.test.ts` for the new per-product signer configuration and test valid signing, missing/bad signature, body tampering, wrong product, unknown key, unknown binding, replay/idempotency, expired timestamp, version mismatch, body limit, rate limit, and secret absence. Preserve all existing relevant denial assertions. Dispatch the continuation in background with a tracked session ID and generous lane timeout; the previous lane was killed by the orchestrator’s 420-second foreground timeout (`CHAIN-FAILURE-T2-ORCHESTRATOR-TIMEOUT-2026-09-20.md:30-35, 68-74`).
WORKER REFUSAL ASSESSMENT: correct — before this classification, choosing A or B would have invented an unresolved wire contract. The worker correctly reported the contradiction, fixed only typecheck, and stopped under the Decision Gap contract.
EVIDENCE READ: Read the four requested T2/manifest documents, `PORTFOLIO_PRODUCTION_MASTER_PLAN.md` at the requested sections, `productEvents.ts`, `signerRegistry.ts`, and the untouched legacy `productEvents.test.ts`. `npx tsc --noEmit` from `apps/hub-web` exited 0. `npx vitest run` exited 1 before tests started because Vitest/esbuild hit `spawn EPERM` in the harness.
UNTESTED AREAS: No successful Vitest execution; no valid new-contract happy-path test; no full twelve-case T2 test evidence; no deploy, database, runtime, or live tamper/replay verification.