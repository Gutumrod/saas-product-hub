# Independent Review — P0a-C1 Portfolio Foundation — 2026-09-03

**Scope:** reassess checkpoint P0a-C1 only. No implementation, merge, deploy, database mutation or secret change.
**Reviewer role:** independent assessor; this pass did not implement the CM01 timezone fix.
**Authority:** `docs/platform/PORTFOLIO_PRODUCTION_MASTER_PLAN.md` revision 3 and `docs/platform/CI_BASELINE.md`.

## Evidence Re-checked
- Published foundation artifacts exist: repository map, runtime matrix, environment/secrets policy, evidence template, CI baseline and P0a reconciliation evidence.
- `hub-web` PR head `d8e31c7` has GitHub Actions run `33593735430` with conclusion `success`.
- CM01 code head `aeaa750` has owning push CI run `33670789635` with conclusion `success`; the post-fix docs head `6202108` also has green run `33671032357`.
- Both proving workflows execute on GitHub-hosted runners after `actions/checkout@v4`. Those jobs start from repository checkout state and cannot depend on the operator's local untracked files.
- The earlier CM01 red run `33128547044` at `ff15819` is preserved as historical evidence and superseded by the green fix run; no assertion was weakened.

## Criterion 2 Assessment
Checkpoint text requires the CI definition to run green on `hub-web` and the first adopter from clean clones with no reliance on an untracked local file.

1. **Green CI:** PASS on both proving repositories.
2. **Clean clone:** PASS. A GitHub-hosted runner plus `actions/checkout@v4` is a fresh checked-out workspace, not the operator's local working tree.
3. **No untracked-local dependency:** PASS. The hosted jobs do not receive the operator's local untracked files and both completed successfully.
4. **Lint placeholder:** NOT a P0a blocker. `CI_BASELINE.md` explicitly defines lint as advisory/absent when a proving repository has no installed linter and records closing that gap as P0b work. Treating the placeholder as a P0a failure would contradict the published P0a CI contract itself.

## Other P0a-C1 Conditions
- Foundation documents are published and unambiguous at the named evidence set.
- P0a item 6 document/code reconciliation is recorded in `PHASE_P0a_B4_EVIDENCE.md`; unresolved per-repository release debt is assigned to P0b rather than hidden.

## Verdict
**PASS — P0a-C1 Portfolio foundation ready.**

This verdict unlocks downstream tracks that were gated only by P0a-C1. It does not waive P0b, product-specific gates, R15, database apply gates, Pawstia admission, or any release/deploy approval.

## Residual Work
- Add real lint tooling per repository under P0b where required.
- Keep dependency/security/release-readiness debt at the repository gates defined by the master plan.