Warning: running headless with --yolo / approval-mode=yolo and no sandbox. All tool calls (shell, write, edit) auto-execute at this process's privilege level. Enable a sandbox via --sandbox / QWEN_SANDBOX, or set QWEN_CODE_SUPPRESS_YOLO_WARNING=1 to silence this notice.
# Qwen ΓÇö Product Gate Expert Input (BK01 Booking)

## Recommendation

**Position BK01 as a Thailand-first, single-location appointment-operations SaaS ΓÇö not a bare booking widget ΓÇö for the deliberately chosen first segment: hair salons, barbers, beauty salons and nail studios with ~1ΓÇô10 service providers, operating in Thailand with LINE as their customer channel.** This is not my invention; it is the owner-approved ICP (PD-001, APPROVED 2026-08-28) and the LOCKED vision (`docs/00_PRODUCT_VISION.md`), and my source-level review found the locked contract internally consistent and largely implemented in the BK-A remediation branch.

**What it should be:** a service-operations product whose core loop is: owner configures shop/services/providers/schedules ΓåÆ publishes a booking URL ΓåÆ customer books collision-safe against real availability ΓåÆ optional PromptPay deposit with private slip upload ΓåÆ merchant (or Pro auto) verification ΓåÆ confirmation ΓåÆ LINE confirmation + pre-appointment reminder ΓåÆ day-of completion/no-show marking ΓåÆ tenant-scoped history/CSV export. Booking is the acquisition surface; the retained value is operational integrity (no double-booking, deposit review, no-show measurement, staff self-scope). The vision's own problem statement supports this: the failure being solved is "keeping staff availability, deposits, confirmations, changes, and customer communication consistent," not merely taking bookings.

**Where V1 ends:** at the locked target contract in `01_PRD.md` (all Required rows) plus the 14 BK-A remediation items, gated by `09_TEST_RELEASE_GATES.md` G0ΓÇôG10, with explicit exclusions (no clinic workflow, no multi-branch, no marketplace/POS/CRM suite, no annual billing, no customer app, no public slip URLs, no `promptpay.io` runtime, blacklist optional-only, tickets demoted to operational capability).

**What is product-defining (keep in V1):** PromptPay/slip deposit loop with fail-safe verification; merchant-owned LINE OA for paid production plus required confirmation/reminder with delivery evidence; explicit staff auth-userΓåÆstaff mapping with self-scope; hold/expiry and customer self-reschedule/cancel. **What is not product-defining:** the ticket/case module (PD-018: operational capability, not a lead feature); CM01 is a separate product and must not be merged into BK01 by name similarity.

**Gate-stability blockers (owner decisions, not for me to decide):** auto-slip provider + allowance + cost/failure policy; final Basic/Pro prices (provisional by PD-003; priced at a later commercial gate); any WSTERA-managed LINE allowance model; and ΓÇö non-owner but equally blocking ΓÇö the absence of an approved PostgreSQL/Supabase runtime, which keeps every DB-backed gate (G2, DB portions of G3ΓÇôG9) unverifiable.

## Verified facts/evidence used

All verified by opening files at current HEAD `213360a` on `feature/bk-a-v1-contract-remediation` (working tree clean; branch ahead of origin `908108c` by one docs commit):

- **Segment and identity:** `docs/00_PRODUCT_VISION.md` (LOCKED 2026-08-28): primary ICP hair/barber/beauty/nail, 1ΓÇô10 providers, Thailand, LINE-first; category "Thailand-first appointment operations SaaS with LINE-assisted customer communication and PromptPay-native deposit workflows"; explicit non-goals list. `docs/PRODUCT_DECISIONS.md` PD-001 APPROVED 2026-08-28 (rejected clinic-first, multi-branch-first, generic-all-services).
- **V1 contract:** `docs/01_PRD.md` (LOCKED, baseline `main @ e99615d`): FR/SEC/REL/OPS/NFR tables with Required/Optional dispositions; "Public-launch blockers" section requires passing traceability for every Required row.
- **Domain rules:** `docs/05_BOOKING_DOMAIN_RULES.md` (LOCKED): canonical booking/deposit state axes; 7-condition availability with fail-closed missing schedules; deterministic Any Staff (lowest same-day qualifying workload + stable tie-breaker); 15-minute hold expiry; deposit=0 explicit no-deposit override; duplicate transaction-reference rejection; no auto no-show inference; tenant-local optional blacklist; Asia/Bangkok.
- **Flows:** `docs/06_UX_USER_FLOWS.md` (LOCKED): customer booking with recovery states, customer change flow (policy-guarded cancel/reschedule with atomic mutation), owner onboarding (first booking = first-value candidate), owner daily flow, staff flow (own schedule/bookings only, missing mapping fails closed), platform operator flow (audited, no impersonation).
- **Roadmap and current state:** `docs/10_DEVELOPMENT_ROADMAP.md`: Stage 4 reconciliation CLOSED at `836943a`, must not be repeated; BK-A items 1ΓÇô14; BK-B pilot (5ΓÇô15 shops), BK-C commercial lock, BK-D launch gated on `09_TEST_RELEASE_GATES.md` + independent PASS + owner authorization. `docs/CURRENT_STATUS.md` (2026-09-03): BK-A remains open; P0a-C1 portfolio gate PASS so BK01 is the eligible next heavy track; DB gates BLOCKED_ENVIRONMENT; next authorized work = legacy RPC authorization, paid-LINE fail-closed, stale-hold reschedule handling, regression tests, fresh CONT-03 review.
- **BK-A evidence:** `docs/audit/BK-A-IMPLEMENTATION-EVIDENCE-2026-08-29.md`: remediation migration `20260829105155_bk_a_v1_contract_remediation.sql` created but not applied anywhere; non-DB gates PASS at `908108c` (19/19 tests, lint 0 errors, both builds pass, static-absence checks for `promptpay.io`/annual/100-500/`α╕¢α╕Ñα╕¡α╕öα╕áα╕▒α╕ó 100%` all pass, secret scan clean); owner-decision blockers listed (auto-slip provider/allowance/economics, managed-LINE model, final prices, nullable fail-closed policy windows).
- **Independent review:** `docs/audit/INDEPENDENT_REVIEW_CODEX_BK-A_2026-08-29.md` (read-only reviewer, HEAD `908108c`): PASS with no P0/P1 code/design defect; verified staff self-scope RLS, private `deposit-slips` bucket with booking-scoped path validation, `customer_reschedule_booking`/`customer_cancel_booking` row-locked atomic transactions backed by the `prevent_overlapping_staff_bookings` EXCLUDE constraint, `classifyAutoSlipResult` auto-confirming only exact positive results, unique partial index on `trans_ref`, LINE HMAC + server-only merchant config, Stripe idempotent/out-of-order-safe sync, `*/5` reminder cron; DB-backed gates marked BLOCKED_ENVIRONMENT (no local PostgreSQL, no Docker permitted).
- **Contradiction register (closed):** `docs/audit/CURRENT_TRUTH_AND_CONTRADICTIONS.md`: 18 P1/P2 baseline contradictions each resolved by an owner decision and mapped to BK-A (ROLE-001 staff scope, SEC-001 public slip bucket, PAY-001 `promptpay.io` QR, PROD-001 reschedule absent, PROD-002 reminders absent, QUOTA-001 100/500 walls, BILL-001 annual UI, ROLE-003 staff ticket access, CLAIM-001/COMM-001 copy). Baseline code truth confirmed: DB exclusion constraint on overlapping active bookings, expired-hold clearing before conflict, public booking only via guarded `create_booking_hold`.
- **Market evidence (cited as evidence only, not deciding competition/pricing):** `docs/audit/MARKET_SOURCE_LEDGER.md` (2026-08-28, refresh-triggered): 11 Thai direct competitors, several with unlimited-booking paid tiers, reminders and self-cancel/reschedule as table stakes; PromptPay scale evidence (BOT May 2026); explicit rule that vendor claims are not BK01 evidence and no no-show-reduction effect is proven.
- **Module Hub / CM01:** the Module Hub capability scan brief is **HOLD ΓÇö not released** (`docs/council-product-destination-2026-09-03-canonical-01/module-scan/COUNCIL-BRIEF.md`), so no Module Hub overlap claim is admissible in this gate at all. Independently verified: `docs/platform/TICKET_SYSTEMS_DISAMBIGUATION.md` + `docs/products/registry.yaml` (CM01 `booking_ticket_module` = standalone React-only template, no backend, own family, explicitly not grouped with BK01 per owner decision 2026-08-26). BK01's ticket system is native full-stack (`supabase/migrations/20260818000000_local_service_tickets.sql`; `apps/booking-admin/src/lib/ticket-domain.ts` header confirms a one-time historical port from CM01's domain code, then diverged).
- **Native capability spot-checks in source:** consumer booking page uses `QRCodeSVG` (qrcode.react) + local `createPromptPayPayload` ΓÇö no `promptpay.io` runtime path (`apps/booking-consumer/src/app/book/[slug]/page.tsx`); the BK-A migration defines `submit_deposit_slip`, `current_staff_id`, `link_staff_user`, `customer_cancel_booking`, `customer_reschedule_booking`, `set_booking_outcome`, `export_core_business_data`, `request_account_closure`, `enforce_ticket_owner_admin`, notification claim/complete functions, and flips `deposit-slips` bucket to private.

## Key reasons

1. **The ICP decision is locked and consistent across every SSOT doc** ΓÇö vision, PRD, domain rules, flows, pricing, roadmap and traceability all describe the same single-location, 1ΓÇô10-provider, Thai/LINE-first shop. No document contradicts PD-001.
2. **Identity as an operations product is grounded in the contract, not aspiration:** the V1-required lifecycle (cancel/reschedule/complete/no-show with audit), deposit review loop, staff scoping and notification-evidence requirements are all "operations" capabilities; a pure booking-link tool would not require any of them.
3. **The product-defining capability set is exactly the differentiator hypothesis the vision locks:** reliable staff scheduling + Thai-native deposit flow + merchant-owned LINE + low setup burden; LINE alone and PromptPay alone are explicitly declared non-defensible because Thai competitors already have them. The locked PRD therefore requires the *combination* to be native: controlled QR (verified native), private slips (implemented, runtime-unproven), merchant-OA boundary (implemented, runtime-unproven), collision-safe scheduling (DB constraint + RPC, runtime-unproven).
4. **V1's end line is unusually well-defined for this stage:** traceability (`docs/audit/FEATURE_REQUIREMENT_TRACEABILITY.md`) assigns every feature a literal disposition (V1 REQUIRED / OPTIONAL / POST-V1 / RETIRED), and the PRD's public-launch blocker rule plus gate model G0ΓÇôG10 convert the finish line into testable evidence rather than opinion.
5. **The locked edge-case set is mature and internally coherent:** fail-closed availability, hold expiry releasing capacity, atomic reschedule under one transaction, duplicate trans-ref rejection, ambiguous auto-slip never auto-confirming, notification failure never mutating booking truth, Stripe idempotency, downgrade preserving history, trial failing closed. These are the rules a Product Gate should treat as already decided ΓÇö reopening them would be scope regression, not diligence.
6. **Nothing product-defining depends on Module Hub:** the scan is on hold, and the only name-adjacent item (CM01) is verified as a disjoint customer base with no live code path; BK01's case module is a native, demoted-to-operational capability (PD-018). The gate can evaluate BK01 on native capability alone.

## Risks / failure cases

1. **"Target contract Γëá shipped product" risk (highest):** every PASS so far is static/unit/build-level. No migration has ever been applied to a database; G2ΓÇôG9 DB-backed evidence (RLS denial, concurrent overlap, webhook ordering, LINE delivery, reminder scheduling, CSV content, audit persistence) is zero. If the DB runtime never becomes available, V1 cannot be evidenced as finished regardless of product-contract quality ΓÇö and a Product Gate verdict today would rest on unverified authority-layer behavior.
2. **Pro's defining paid capability has no provider:** auto-slip automatic verification is V1-required-before-Pro-sale (PD-004), yet provider, allowance, unit cost, top-up price and failure policy are all explicitly PENDING owner decisions (`04_PRICING_ENTITLEMENTS.md`, BK-A evidence). If unresolved, Pro cannot be sold as specified, and the plan matrix is unstable at its most expensive tier.
3. **CURRENT_STATUS vs. independent-review tension:** the independent review at `908108c` reports PASS/no P0-P1, while `CURRENT_STATUS.md` (committed after, at `213360a`) still lists BK-A as open with named remaining items (legacy RPC authorization, paid-LINE fail-closed, stale-hold reschedule). Either the status text lags the review or residual remediation exists; a stable gate needs this reconciled in the CONT-03 closeout.
4. **Merchant policy windows ship fail-closed with no defaults:** cancel/reschedule windows are nullable until each merchant configures them. A merchant who never configures them gets a customer-facing change flow that always fails; onboarding must force configuration or V1's self-service promise is hollow in practice.
5. **Segment-scope discipline risk:** the traceability rule ("no feature without a row") is strong on paper, but the market ledger shows competitors bundling CRM/calendar-sync/waitlist; pressure to add these pre-pilot would violate the locked V1 end line and re-bloat scope.
6. **Claim integrity risk:** static absence checks verified at code level only (DB-backed authority evidence pending). The `α╕¢α╕Ñα╕¡α╕öα╕áα╕▒α╕ó 100%` / annual / 100-500 copy regressions are exactly the failure class the evidence-constrained marketing rule exists to prevent; regression tests exist, but a single stale locale string would resurrect a P1.

## Assumptions

- "First deliberate service-business user segment" = the first segment chosen deliberately at product level, i.e., PD-001's hair/barber/beauty/nail single-location ICP; I treat earlier untracked exploratory work as superseded because PD-001 explicitly rejected clinic-first and generic alternatives.
- The owner decisions approved 2026-08-28 remain in force; no later ADR or dated decision update contradicts them (I found none in the repo).
- Stage 4 reconciliation closed at `836943a` is final and out of scope per the brief; I did not re-derive it.
- The Docker prohibition on the active Windows host stands, so DB gates remain blocked until an alternative approved PostgreSQL/Supabase runtime exists.
- The independent review's PASS applies to code/design only; I treat DB-level truth as unproven, matching the reviewer's own BLOCKED_ENVIRONMENT labeling.

## Open questions / missing evidence

1. **Auto-slip provider decision:** which provider, what monthly allowance, what unit economics/top-up, what operational failure policy ΓÇö all explicitly PENDING; without them the Pro tier of the V1 contract is not fully decidable.
2. **Final Basic/Pro prices:** PD-003 keeps α╕┐490/α╕┐990 as pilot reference only; the price-lock gate requires implemented contract + variable-cost model + pilot WTP + competitor refresh + owner approval. (This is an owner decision downstream ΓÇö I do not decide it here ΓÇö but its unresolved state prevents a stable commercial framing of the V1 bundle.)
3. **Managed LINE allowance model:** whether WSTERA ever bears message cost, and under what explicit allowance (BK-A owner-decision blocker; also flagged in `04_PRICING_ENTITLEMENTS.md`).
4. **Blacklist final disposition:** V1 OPTIONAL with TBD entitlement; does the owner intend to ship it in V1 at all, or formally defer to POST-V1?
5. **DB runtime resolution:** when and on what host will the approved PostgreSQL/Supabase runtime exist so G2ΓÇôG9 can run? Every integrity/tenancy/money claim in the V1 contract currently hangs on this.
6. **CONT-03 closure reconciliation:** what exactly remains under "legacy RPC authorization and paid-LINE fail-closed behavior first, then stale-hold reschedule handling" per `CURRENT_STATUS.md`, and how it relates to the independent review's PASS at `908108c` ΓÇö the record should state whether these are pre-review items already fixed or post-review follow-ups.
7. **BK-B pilot evidence does not yet exist:** time-to-first-value, booking integrity under real concurrency, deposit/notification reliability and willingness-to-pay are all declared hypotheses-not-yet-measured (`00_PRODUCT_VISION.md` "Success evidence"); the Product Gate cannot treat them as established.
8. **Module Hub overlap:** unanswerable by design until the owner releases the HOLD scan; when released, candidates must be inspected against the native capabilities already verified here (QR generation, ticket/case domain, notifications) rather than by name similarity.

## Confidence 0-100

**84.** Rationale: the segment, identity, core loop, V1 finish line, exclusions and locked business rules are verified directly against LOCKED SSOT docs at HEAD `213360a`, and the product-defining capabilities are verified as native in source (migration functions, private-bucket migration, QR implementation, ticket-domain provenance, CM01 disambiguation) plus an independent code-level review at `908108c` ΓÇö this is a high-consistency, well-evidenced contract. The 16-point discount reflects: (a) zero DB-backed runtime evidence for the authority layer the product's integrity claims depend on, (b) the unresolved auto-slip provider/cost decision that makes the Pro tier's defining capability undecidable, (c) provisional prices and the undecided managed-LINE model, and (d) the unresolved tension between `CURRENT_STATUS.md`'s open-item list and the review's PASS, which only the CONT-03 closeout can reconcile.