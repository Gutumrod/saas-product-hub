Warning: running headless with --yolo / approval-mode=yolo and no sandbox. All tool calls (shell, write, edit) auto-execute at this process's privilege level. Enable a sandbox via --sandbox / QWEN_SANDBOX, or set QWEN_CODE_SUPPRESS_YOLO_WARNING=1 to silence this notice.
All evidence is gathered and cross-checked. Composing my final independent answer now.

---

# Expert Answer ΓÇö CM01 Product Gate (independent)

## Recommendation

CM01 should be defined as: **a one-time-source, local-first, Thai-first React case-management UI module** (intake ΓåÆ history/search ΓåÆ detail/act/close/reopen ΓåÆ manual retention cleanup), sold as source code to **frontend developers and agencies** who embed it in their own client projects and skin it via the host-configurable theme contract. It is **not** a deployable end-user product, **not** a hosted SaaS, and **not** a backend capability.

**What CM01 is:** a bounded, reusable frontend capability ΓÇö a working, tested reference implementation of a case-management workflow behind a `TicketRepository` adapter seam.

**For whom:** frontend devs/agencies embedding case handling into client work (per `docs/platform/TICKET_SYSTEMS_DISAMBIGUATION.md` ┬º#2, owner-approved 2026-08-26). The single in-app role is "α╣Çα╕êα╣ëα╕▓α╕½α╕Öα╣ëα╕▓α╕ùα╕╡α╣êα╕öα╕╣α╣üα╕Ñα╣Çα╕äα╕¬" (case officer) ΓÇö but the *buyer* is the developer/agency, not the officer. The officer is the end-user persona the buyer's client resembles.

**Where V1 ends:** V1 ends at what exists at HEAD `6202108` ΓÇö exactly the PRD scope with zero additions. The finish line is the PRD ┬º1 success criteria, all implemented and verified: single-officer create/search/view/act/close/reopen without login; search by phone/name/Ticket ID with phone normalization and prior-history notice *without autofill*; overdue visible/filterable without auto status change; Thai default + English toggle; persistence + reset-to-seed; UI/domain decoupled from localStorage. The PRD ┬º8 out-of-scope list **is** the V1 boundary and should stay so until a buyer signal or owner decision changes it.

**Backend capability:** **not required for a usable V1.** The localStorage adapter delivers the complete officer workflow today; the `TicketRepository` contract (`src/data/repository.ts`) is designed so a Supabase/REST adapter is a post-V1 swap. Backend is a post-V1 revenue/roadmap item, and per the disambiguation doc even that is an open proposal ("wiring to modules-hub `ticket-tracker`" vs PRD ┬º9's "Supabase adapter") ΓÇö two parent documents suggest two different backend paths, neither decided.

**Defensible value beyond generic dashboard/template UI:** concrete workflow mechanics, not chrome ΓÇö phone normalization with prior-customer notice-without-autofill; overdue computed, never persisted, never auto-closing; a three-step destructive retention flow (preview ΓåÆ confirmation ΓåÆ type exact `DELETE`, Closed-only, 12-month cutoff); a semantic-token theme system with host lock mode (`defaultTheme`/`allowThemeSwitch`); Thai/English i18n with persisted preference; stable English internal enums/payloads; 63 unit tests + 5 Playwright E2E suites; timezone-pinned CI on a green baseline.

## Verified facts/evidence used

- **Git:** HEAD = `6202108753d6009dcad39da0cae954cc12f4d6eb` ("docs: record post-fix CI reality for CM01"), tree clean, `main == origin/main`. Lineage: `fdf6608` (initial) ΓåÆ `be37b0a` (theme system) ΓåÆ `ff15819` (CI baseline) ΓåÆ `aeaa750` (TZ pin) ΓåÆ `6202108` (docs-only). Note: `docs/CURRENT_STATUS.md` and `implementation_plan.md` still snapshot HEAD as `aeaa750`; the delta is the docs-only `6202108` commit ΓÇö no code difference.
- **CI reality (read from `.github/workflows/ci.yml`):** blocking = install (`npm ci`), typecheck, test, build; lint stage commented out (`.eslintrc.cjs` exists; `package.json` has no eslint dep and no `lint` script ΓÇö verified in `package.json`); `npm audit || true` advisory; license/secret/SAST stages commented out with CEO-decision markers; artifact upload advisory (14-day retention). The comment block itself documents the RED run `33128547044` history and P0b item CM-D.
- **Timezone fix verified in source:** `vite.config.ts` pins `test.env.TZ = 'Asia/Bangkok'` with the rationale comment; `src/__tests__/timezone.test.ts` exists; suite = 13 test files incl. the guard (`docs/CURRENT_STATUS.md` + `docs/daily/2026-09-03.md`: 63/63 green, root cause was harness not date semantics).
- **Product identity:** `docs/products/registry.yaml` lines ~430ΓÇô459: `product_code: CM01`, `delivery_model: one_time_source_product`, `deployment_model: source_product`, `commercial_status: prototype`, category "Operations & Scheduling", acceptance `architecture: true, operations/commercial/support: false`. Explicit comment: own family, NOT grouped with booking/BK01.
- **Boundary decision (owner-approved 2026-08-26):** `docs/platform/TICKET_SYSTEMS_DISAMBIGUATION.md` ΓÇö four systems: CM01 (React SPA only, no backend, localStorage-only adapter, host theme system, dev/agency audience), TT01 `products/ticket-tracking-relay` (full-stack public issue tracker, port of ticket-tracker #3), Module Hub `ticket-tracker` (backend-only library, no UI/auth/tenancy), booking's native ticket system #4 (full-stack Supabase RPC/RLS, bundled, never sold separately; its `ticket-domain.ts` header cites "Ported from products/booking-ticket-module/src/domain/ΓÇª" ΓÇö historical ancestor, then diverged; 2026-08-21 same-day-retracted bundling decision in ROADMAP ~line 167).
- **Scope/UX/business rules:** `PRD.md` ┬º2 (single role, no auth by design), ┬º4 (12 rules incl. overdue = computed not persisted; retention deletes Closed only, manual), ┬º7 (Thai-first, 4 theme presets, two-step destructive confirm, 375px mobile), ┬º8 (9 out-of-scope items), ┬º9 (future roadmap incl. Supabase adapter). `README.md`: three hash routes `#/new`, `#/tickets/:id`, `#/history`; seed `TKT-0001..0003`; recheck payload local-only, never transmitted; explicit non-claims section (no deployment, no cross-browser claim, Chromium-only E2E).
- **Architecture boundary:** `src/data/repository.ts` ΓÇö `TicketRepository` interface (9 methods incl. `search`, `findByNormalizedPhone`, `previewRetention`, `deleteClosedBefore`, `reset`); `createLocalStorageRepository()` is the sole implementation (grep: only App.tsx + tests reference it). `src/components/**` has zero `localStorage` matches ΓÇö the PRD ┬º1 "UI/domain not coupled to localStorage" criterion holds at component/service level.
- **Injection nuance:** `src/App.tsx:14` hard-codes `const repo = createLocalStorageRepository()` at module scope. Theme is host-configurable via props + `window.__BOOKING_TICKET_THEME_CONFIG__` (`docs/THEME_INTEGRATION.md`), but the repository is not prop-injectable ΓÇö swapping adapters currently requires a one-line source edit, not configuration.
- **Test/E2E inventory:** `src/__tests__/` = 13 files across domain/data/services/i18n/navigation/theme/timezone; `e2e/` = intake, history, retention, theme, ticket (Chromium).
- **Release 1A scope:** `docs/council-product-destination-2026-09-03-canonical-01/briefs/01_RELEASE-1A-PRODUCT-GATE.md` ΓÇö Product Gate only for 7 products incl. CM01; Business/Market Gate, Module Hub scan, Portfolio Arbitration, Architecture, Risk, Pre-Build, Agent Relay all NOT released. Module Hub scan `COUNCIL-BRIEF.md`: HOLD, read-only, similar names Γëá fit.
- **Revenue doc status:** `REVENUE-STRATEGY.md` (cited by the disambiguation doc for the $39/$129 template idea) is an **untracked working draft, not approved pricing** per `docs/platform/DEEP-VERIFICATION-2026-08-18-CONSOLIDATED.md` ┬º5.
- **Stack:** `package.json` ΓÇö React 18.3.1, TS 5.5.3, Vite 5.3.4, Vitest 2.0.5, Playwright 1.45.3; only react/react-dom runtime deps. `implementation_plan.md`: 6 phases all done, non-goals = auth, Supabase, notification, binary upload, deploy.

## Key reasons

1. **The artifact's actual shape matches "source product" exactly.** No server, no deploy target, no `.env.example` (implementation_plan ┬º10), no production claim ΓÇö but a complete, tested, documented UI with an explicit adapter contract. A deployable product or SaaS identity would require building what the PRD explicitly excludes; a pure "template" label would understate the enforced `TicketRepository` contract and the test coverage.
2. **The buyer/use flow is already owner-pinned.** The disambiguation doc is code-verified and owner-approved: audience = frontend devs/agencies licensing a case-management UI for their own client work; the 2026-08-21 bundling-into-booking proposal was retracted same-day once booking's native system #4 was found superior for booking's use. CM01's buyer flow: purchase source ΓåÆ clone ΓåÆ embed `App` with theme props (or `window.__BOOKING_TICKET_THEME_CONFIG__`) ΓåÆ officer's clients use the three pages ΓåÆ buyer later swaps in a backend adapter by implementing `TicketRepository`.
3. **V1 already ends in the right place ΓÇö the risk is scope creep, not a gap.** Every PRD success criterion is implemented and CI-verified. The PRD ┬º8 out-of-scope list and ┬º9 roadmap are clean, owner-authored boundaries. There is no evidence of a missing V1 feature; there is evidence of finished scope.
4. **Backend is a seam, not a dependency.** All 9 repository methods are contract-typed; components never touch storage; the future adapter is a swap at one import site. Making backend a V1 requirement would contradict the PRD, the registry description ("No backend yet ΓÇö designed for a future Supabase/REST swap"), and the disambiguation doc.
5. **Boundary evidence is code-level, not name-level.** Booking's `ticket-domain.ts` header proves common ancestry and divergence; no live shared code path exists. TT01 is full-stack with a different buyer (public issue tracking); ticket-tracker is backend-only with no UI. The four systems have disjoint buyers per the disambiguation matrix. Nothing in the current repo contradicts the separation; therefore it must be preserved.
6. **Differentiation is in workflow semantics.** Prior-customer notice without autofill, overdue-without-auto-status-change, and preview-confirm-DELETE retention are opinionated, documented product behaviors with tests ΓÇö not dashboard chrome a generic React admin template provides.

## Risks/failure cases

- **Adapter injection gap:** the "swap the backend" story is a one-line source edit in `App.tsx` today, not a config option. For a source product this is workable, but if any buyer-facing claim says "drop in your backend without touching module code," it would be false as wired. (Decision gap, not a defect.)
- **Cross-timezone buyer exposure:** retention cutoff and overdue are evaluated in the *viewer's local* calendar day (documented in `vite.config.ts` comment and CURRENT_STATUS). A buyer's host in a different timezone gets different day-boundary semantics than the seed fixtures assume. Deliberate, tested behavior ΓÇö but a template buyer may not expect it; documentation should carry the caveat.
- **Single-browser data reality:** localStorage-only means clearing storage/another browser isolates or destroys data, corrupted storage falls back to seed (README non-claims). Officers' real client data living in one browser is fragile; this is by-design for V1 but is the most likely buyer complaint.
- **Docs snapshot drift:** CURRENT_STATUS.md/implementation_plan.md cite `aeaa750` while HEAD is `6202108`. Docs-only drift, self-documented in the clerk brief ΓÇö but it is a standing "verify CI reality yourself" obligation for every future gate.
- **Unresolved CI debt is buyer-relevant:** no lint gate, dev-toolchain audit findings (1 moderate/3 high/2 critical in playwright/vitest, advisory), license/secret/SAST stages CEO-open. For a product sold as source, unscanned dependencies shipping in the artifact is a latent commercial risk flagged in ci.yml comments itself.
- **"super-admin" theme preset in a sellable artifact:** the fourth theme preset is named after this portfolio's internal concept; a third-party buyer inheriting a "Super Admin" theme is a minor branding leak ΓÇö cosmetic, no code change proposed here (read-only).
- **Identity-confusion risk in the market:** the repo/directory name (`booking-ticket-module`) invites mis-grouping with BK01 despite the owner decision. Tagline/registry are correct; the *path name* remains the confusion vector.
- **Failure case to avoid:** treating Module Hub `ticket-tracker` as a merge candidate because of the name. Per scan rules, similar names are not evidence of fit, and the scan is on HOLD.

## Assumptions

- GitHub Actions runs `33670789635`/`33670273164` are green as recorded in CURRENT_STATUS/daily log; I verified the CI *configuration* and test source directly but did not query the Actions API from this session.
- The owner-approved status of `TICKET_SYSTEMS_DISAMBIGUATION.md` (2026-08-26) and the registry comments stands as of today; I found no later contradicting owner decision.
- "V1" means the PRD-approved MVP scope as implemented ΓÇö not the future roadmap items ΓÇö consistent with `commercial_status: prototype` and acceptance flags.
- The PRD's Thai role term refers to a back-office case officer at a shop/service business; the buyer is the dev/agency building for such businesses (per disambiguation ┬º#2).
- `$39/$129` pricing references in parent docs are proposals, not decisions; I assign them no weight.

## Open questions/missing evidence

1. **Backend adapter direction (owner decision):** PRD ┬º9 says Supabase adapter + Storage; disambiguation ┬º#2 proposes wiring to modules-hub `ticket-tracker` instead ("a proposal, not a plan in motion"). Two parent documents point different ways; the owner has not chosen. This is the single largest unresolved identity question for post-V1 CM01.
2. **Should a `repository` prop / injectable adapter be added pre-sale**, or is the one-line `App.tsx` edit the documented integration point? Affects whether "reusable capability" claims are configuration-level or source-edit-level.
3. **Theme preset naming:** is `super-admin` acceptable to ship to external buyers, or should the preset be renamed before any sale? (Owner decision; not actionable under read-only rules.)
4. **Commercial readiness path:** registry acceptance says `commercial: false, support: false`; how CM01 reaches sellable state (licensing text, purchase flow, support) is outside this gate's release scope (Business/Market Gate not released in 1A) and remains unowned.
5. **Chromium-only E2E:** no cross-browser verification claim exists (README). Acceptable for V1, but buyer expectations should be set explicitly.
6. **P0a-C1 reassessment** at portfolio level is explicitly deferred to someone other than this fix's implementer (CURRENT_STATUS "Next Authorized Action: none inside this repo").

## Confidence 0-100

**86.** The product identity, buyer, V1 boundary, and CM01-vs-BK01/TT01 separation are all strongly evidenced at code level and consistent across PRD, README, implementation plan, registry, disambiguation doc, CI config, and test inventory. The remaining uncertainty is concentrated in: (a) I could not independently re-verify the green Actions runs from this session; (b) the unresolved backend-adapter direction (Supabase vs ticket-tracker) leaves post-V1 identity genuinely open; (c) small decision gaps (repository injection, theme preset naming) could change the buyer-facing story without changing the artifact itself.