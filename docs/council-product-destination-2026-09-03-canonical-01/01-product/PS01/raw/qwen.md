I now have a complete evidence picture. Here is my independent council assessment.

---

# PS01 Pawstia — Qwen Independent Expert Assessment

## 1. Recommendation

**Keep Pawstia as a narrow, single-store "Pet Hotel/Daycare OS" — not a broader PMS — and hold V1's boundary exactly where the PRD draws it.** The product's correct identity is: *the room-matrix + 15-second LINE Daily Report + Google Sheets data-ownership system for small single-location Thai pet hotels/daycares, sold to the store owner, operated on iPad by low-tech staff, consumed by pet parents inside LINE only.*

- Do **not** broaden toward a generic pet-business PMS (grooming, clinic, retail, multi-branch) — the PRD's own non-goals are correct and load-bearing, and no demand evidence exists to justify expansion.
- Do **not** narrow V1 below the four product-defining pillars (booking anti-collision, Daily Report→LINE, LINE identity claim, Google Sheets replica) — three of them are literally inside the positioning statement and the objection-handling scripts.
- Treat everything beyond those pillars (visitor camera, LIFF self-booking, CSV import tooling, subscription/quota machinery) as optional breadth, not as the V1 definition.
- The next value milestone is **one real store running the daily loop**, not payment integration and not further features. Payment collection must stay out of V1/beta scope; the subscription lifecycle work (Phase 13) is correct as *pre-wiring*, but no money path should be built until real-store beta produces willingness-to-pay evidence.
- Do not treat Phase 13's green CI run or technical closure as market fit, and do not treat Phase 13 as finally "closed" until an independent review verdict exists (it does not yet).

## 2. Verified facts/evidence used

**Product identity documents (read directly):**
- `docs/PRD.md` — "Authoritative Target Contract (Final Hardened Edition)": Pet Hotel & Pet Daycare Management OS, Single-Store Focus; positioning statement "Pet Hotel OS ที่จัดการห้อง การเข้าพัก และ Daily Care Report ผ่าน LINE โดยร้านยังมีสำเนาส่งออก…Google Sheets"; 3 named pains (double booking, daily-report chaos, data lock-in fear); explicit non-goals: clinic/pharmacy, grooming queue, SlipOK/billing/e-Tax, Google Drive photo sync, digital pet passport/full multi-camera, multi-branch; core loop diagram (check-in → no-collision room matrix → care info → 15-second report → owner's LINE).
- `docs/PRODUCT_ONE_PAGER.md` — marked `DRAFT — DO NOT PUBLISH`; pricing Starter 990 / Pro 1,490 / Enterprise 2,490 THB; Founding-10 offer (30-day free trial + free import worth 3,000–5,000 THB); LINE OA handle `TBD — official handle not claimed`, production web address `TBD`.
- `docs/BUSINESS_MODEL.md` — target market "Pet Hotels & Pet Daycare Centers (Single-Location)"; Decision C2 Founding Member (Pro entitlement at 990 THB forever, non-transferable, excludes future add-ons) marked "CEO Decision Locked"; hypotheses H1–H4 (30-store pain interviews; >40% trial→paid; WTP at 990–1,490; B2C add-on take-up) all framed as *to validate*, none marked done.
- `docs/SALES_PLAYBOOK.md` — founder-led outreach to ~50 Bangkok-metro pet hotels via LINE/Facebook/cold call; scripts lead with the daily-report pain, then double-booking, then lock-in fear (Sheets replica is the answer to objection #3).
- `docs/ROADMAP.md` (reconciled 2026-08-28) — Stage A Core Product (implemented) → Stage B Closed Beta ("Do not call technical PILOT READY the same thing as successful real-world beta validation") → Stage C Paid Launch → Stage D Expansion; explicit rule "Phase 13 must not be marked CLOSED until…".
- `README.md` — "Pet Hotel และ Pet Daycare แบบ Single-Store Focus in V1"; Not-Yet-Commercially-Complete list: payment, production gates, legal/trademark, real-store beta, multi-branch/grooming/vaccine/advanced camera.

**Implemented product surface (verified in source, not just docs):**
- `supabase/migrations/` — 13 migrations, Phase 2 gateways → Phase 13 subscription. I read `SYSTEM_ARCHITECTURE.md` schema: GiST `prevent_double_booking` exclusion constraint on `bookings`; pet no-overlap invariant; deterministic lock ordering; `daily_reports` with dual idempotency (`(shop_id, idempotency_key)` + `line_delivery_retry_key`), 1–4 photo cardinality, LINE delivery lifecycle (pending→sending→sent/failed, lease recovery); `sync_queue` transactional outbox.
- `supabase/migrations/20260825141500_phase13_subscription_lifecycle.sql` — `shop_subscriptions` (8-state lifecycle, `commercial_offer IN ('standard','founding_member')`, `founding_member_requires_starter` check), `subscription_audit_log`, `get_shop_effective_entitlement()`; `20260825141600_phase13_subscription_hardening.sql` — commercial-mutation blocking and quota triggers. Starter 10 rooms/300 pets enforced at DB boundary per PRD §11 and brief §7.
- `app/` routes verified: `/` operations UI (`app/page.tsx`, `app/operations-client.tsx`), `/dashboard`, `/onboarding`, `/line/book` (LIFF self-booking), `/line/claim`, `/camera/[shopSlug]`, `/auth/accept-invite`, internal LINE-dispatch and Google-sync worker endpoints.
- **Payment absence verified by direct grep:** no Stripe/Omise/PromptPay/SlipOK code in `app/`/`lib/` (initial grep hits were the word "Promise" — false positives; `promptpay|slipok` returns zero files). Matches `docs/COMMERCIAL_READINESS.md`: "Payment collection absent… by design".
- Test surface: `tests/phase3…phase13*.test.ts` + `tests/e2e/phase10-pilot.spec.ts`; `supabase/tests/` pgTAP suites incl. `phase13_subscription_lifecycle.sql`, `phase13_lifecycle_matrix.sql`, `phase13_csv_atomicity.sql`.

**Verification/CI evidence (current, on disk):**
- Branch `verify/phase13-closure-2026-09-01` @ `fdd10e7`, clean tree; commits `f48637d` (legacy `trial`→`trialing` constraint-order fix), `d6f4acf` (Phase 7 fixture determinism), `6527987`/`fdd10e7` (closure evidence docs).
- `.github/workflows/phase13-verification.yml` — full matrix: isolated Supabase, clean replay, DB lint, Phase 1–3 historical regressions at their own migration versions, current-schema pgTAP suites, Phase 13 quota concurrency races (`scripts/phase13-concurrency.sh`), legacy-trial normalize probe, all TS suites, Phase 10 E2E, typecheck/lint/build/whitespace.
- `PHASE13_IMPLEMENTATION_EVIDENCE.md` (dated 2026-09-03) — CI run [33743691064] **success**; documents the discovered defect (legacy check constraint ordering) and its fix; states explicitly: "isolated CI evidence, not production deployment evidence. No PR was merged, no remote database migration was applied, and no deployment occurred. PR #4 remains Draft/Open."
- `docs/daily/2026-09-01.md` — prior failing run 33494605562 (Phase 1 isolation staging failure) and the evidence-first discipline; `docs/daily/2026-09-02.md` + `WORK-BRIEF-2026-09-02.md` — state as of 09-02 (now superseded by the 09-03 evidence).
- **Gap:** no `REVIEW-phase13-*.md` exists (unlike phases 9–12 which each have independent review verdicts) — Phase 13's final independent review is still pending.
- **Doc drift verified:** `docs/CURRENT_STATUS.md` and `docs/IMPLEMENTATION_STATUS.md` (both 2026-09-02) still claim "CI failing at Phase 1 isolation" and "PHASE13_IMPLEMENTATION_EVIDENCE.md does not yet exist" — both now contradicted by on-disk evidence. `docs/ONBOARDING_SOP.md`, `docs/daily/2026-09-01.md`, and `REVIEW-phase12-final` still use the "PawSpace" name.

**Prior phase reviews (read):** `REVIEW-phase12-final-2026-08-23.md` (PHASE PASSED; found 2 real gaps post-verdict: phone-normalization aborting atomic batches, CSV formula injection into live Google Sheets — both fixed), Phase 10 evidence (8/8 real-browser E2E incl. cross-tenant rejection; Issue #3 closed with real email invite flow), Phase 11 evidence (45/45; implementer self-declared pass was corrected by independent review — evidence discipline works), Phase 12 evidence (92/92 + 9/9 E2E; Module Hub import-export = "ADAPTER ONLY / SOURCE SUBTREE COPY").

**Operations/legal/brand state (read):** `docs/PRODUCTION_OPERATIONS.md` — "Pre-Production Readiness Document"; staging required before release; monitoring/backup/support all unchecked. `docs/TERMS_AND_PRIVACY.md` — `DRAFT`, PDPA framework, legal entity "TBD before production", subprocessor regions unverified, 30-day post-termination retention, photos in public-CDN bucket with UUID paths. `PHASE10_PILOT_RUNBOOK.md` — one-store scope; first-owner bootstrap is operator-assisted (no self-service signup); per-shop LINE tokens via `LINE_CHANNEL_ACCESS_TOKENS_JSON` env; daily opening/closing smoke checks; §10 limitations list.

**Module Hub (briefly inspected per brief):** `modules-hub/INDEX.md` + `REGISTRY.md` — `subscription` 0.1.0 and `payment` 0.1.0 exist as completed modules. Phase 13 brief's gate verdict (verified against real hub state): subscription `ADAPTER ONLY` (documented gaps: no automatic grace transition, entitlement stays active for past_due by generic behavior), audit-log `ADAPTER ONLY`, payment `NOT NEEDED`. Pawstia built its own authoritative subscription domain; proven Pawstia-native behavior (booking invariants, LINE delivery lifecycle, Sheets proof-of-control) is not displaced by naming similarity. The only module actually copied is `import-export` (Phase 12, owned and adapted inside `lib/import-export/`).

## 3. Key reasons

1. **The positioning statement is coherent, differentiated, and consistently carried through every artifact.** The one-pager, PRD, README, sales scripts, and onboarding SOP all tell the same story: rooms don't collide, reports reach the pet parent in LINE within seconds, and the store keeps a data copy in its own Google Sheets. That consistency is rare and should be preserved, not diluted.
2. **The daily report is the retention engine; the Sheets replica is the adoption wedge; the room matrix is the integrity floor.** Sales scripts open with the report pain (emotional, B2C-visible, word-of-mouth), handle lock-in fear with the replica (trust), and close with anti-collision (operational truth). This maps exactly to what is built and hardened (GiST exclusion, dual idempotency, proof-of-control binding) — these four pillars are product-defining.
3. **Broadening has no demand evidence; the PRD's non-goals are documented reasoning, not omissions.** Grooming (resource-scheduling of people vs rooms), clinic (medical workflow), multi-branch, e-Tax are each deferred with reasons in PRD §2 and BUSINESS_MODEL §2. The buyer segment (single-store, 990–1,490 THB/month budget) matches the implemented scale (single shop per tenant, operator-assisted onboarding, env-configured per-shop LINE tokens — acceptable at 10 stores).
4. **Narrowing further would break the commercial story.** Removing Sheets sync or the LINE claim flow would gut the differentiation; removing LIFF self-booking or CSV import would only trim scope modestly but is unnecessary since both are already built and verified. The smallest *defensible* V1 is the current Stage-A core minus camera (the only feature with no positioning or sales-script role — bounded visitor camera is the clearest "optional breadth" item).
5. **The V1 boundary already drawn is the right one, and the repo enforces it.** Zero payment code exists (verified), Phase 13's non-scope list explicitly bans payment SDKs/checkout/fake upgrade flows, and `COMMERCIAL_READINESS.md` warns not to read the subscription schema as revenue capability. This discipline should be held through beta.
6. **Paid-launch prerequisites are correctly separated from beta prerequisites** across ROADMAP Stage B/C, PRODUCTION_OPERATIONS §7, COMMERCIAL_READINESS, and the sell-ready brief (`docs/BRIEF-sell-ready-execution.md`: PS-SR-01 → staging → real-store beta → commercial contract → payment → launch). The ordering "beta before payment automation" is explicit and correct.
7. **The evidence culture is unusually strong.** Independent reviews caught real defects (Phase 11 self-declared pass corrected; Phase 12 found a data-loss bug and a security injection); Phase 13 verification refused to weaken a failing assertion and instead found and fixed a real legacy-data defect (`f48637d`). This supports trusting the technical claims — while the same culture correctly refuses to conflate them with market validation.

## 4. Risks/failure cases

1. **Zero real-world validation (highest risk).** Every piece of evidence is synthetic (CI, local E2E, fixture stores). No real store has ever onboarded, sent a real daily report, or hit a real LINE outage. H1–H4 are untested; "ห้องชนช่วงเทศกาล" and "แชทแตก" are hypotheses from the founder, not measured demand. The likeliest failure mode is launching more engineering while the actual gate — one store's daily habit — never gets measured.
2. **Documentation drift creating premature-closure risk.** `CURRENT_STATUS.md`/`IMPLEMENTATION_STATUS.md` (09-02) contradict the 09-03 closure evidence; no independent `REVIEW-phase13` verdict exists. If the staleness isn't reconciled, "Phase 13 CLOSED" can be claimed on evidence-doc alone, and the Roadmap's own gating rule ("must not be marked CLOSED until…") can be quietly bypassed. Also a second-order risk: the green CI run is known to me only through the evidence document + workflow file; I could not re-execute GitHub Actions from this environment.
3. **Founding Member price lock made before WTP validation.** Committing 990 THB/month Pro-level *forever* (C2, "locked") to the first 10 stores caps upside before H3 answers what the market will pay, and the >40% trial→paid target is aggressive for an unproven segment. If beta reveals higher WTP or different packaging, the promise is already public in sales scripts.
4. **Operator-assisted scaling mechanics.** First-tenant bootstrap is manual; per-shop LINE tokens live in server env JSON; new stores require deploy-touching configuration. Viable for a 10-store founding cohort, but a hidden operations cost the pricing doesn't model — and a risk if beta demand arrives faster than manual onboarding capacity.
5. **Privacy exposure of real customer data in beta.** Daily-report photos sit in a public-read CDN bucket (UUID-protected), retention is 30 days post-termination, ToS/Privacy are DRAFT, the legal entity and subprocessor regions are TBD, and PDPA review is pending. Real-store beta means real pet-owner personal data flowing through unreviewed legal ground — the Sheets replica partially mitigates lock-in fear but also widens the data surface.
6. **Brand/trademark clearance is a live business blocker** (one-pager `DRAFT — DO NOT PUBLISH`, LINE OA handle unclaimed, production address unconfirmed, brand-collision issue referenced in Phase 12 evidence). Outreach under "Pawstia" before clearance risks rework or conflict.
7. **No always-on CI.** `.github/workflows/` contains only the PR/dispatch-triggered Phase 13 verification workflow; regressions merged to `master` between verification runs are not automatically caught.
8. **Single-worker Sheets sync (concurrency=1)** and LINE rate behavior are contractually handled but unproven under real store load (e.g., a 40-occupied-room holiday with mass report sends).

## 5. Assumptions

- CI run 33743691064's "success" is taken from `PHASE13_IMPLEMENTATION_EVIDENCE.md` plus the workflow definition on disk; I did not (and cannot, from this environment) re-run or query GitHub Actions myself.
- I treat the PRD/SYSTEM_ARCHITECTURE as the authoritative contracts per the repo's own Source-of-Truth table (`README.md`), and treat `CURRENT_STATUS.md` as the overlay it claims to be — hence its 09-02 staleness is noted but not authoritative.
- I assume the founder/CEO decisions referenced as "locked" (Decision C2, design C Warm Hospitality) are genuinely owner-issued; the repo records them as such.
- I assume the target market remains Bangkok-metro Thai pet hotels as the sales playbook states; nothing in the repo contradicts this.
- I assume "PMS" in the brand name is marketing nomenclature (per README: "Pet Management System by WSTERA"), not a scope claim — the actual contract scope is the Pet Hotel/Daycare OS.
- Module Hub was inspected read-only for overlap only; per the brief, its `subscription`/`payment` modules' existence does not displace Pawstia's native domain behavior, and no cross-repo import was verified anywhere in the codebase (only the Phase 12 owned copy of `import-export` inside `lib/`).

## 6. Open questions/missing evidence

- **Independent Phase 13 review verdict** (`REVIEW-phase13-*.md`) — the only missing piece of the Phase 13 closure gate as defined by the repo's own rules; also reconciliation of `CURRENT_STATUS.md`/`IMPLEMENTATION_STATUS.md` to the 09-03 reality.
- **Who is store #1?** The beta sequence ("1 real store, then 3, 5, 10") has no named candidate, no signed trial agreement, no success criteria for the first cohort's go/hold/rollback decision (PS-SR-06 defines the metrics but no store exists to measure).
- **Willingness-to-pay and pain-intensity data** — H1/H3 require real interviews; nothing on disk shows even one.
- **Payment provider decision and commercial transition rules** (upgrade/downgrade, trial expiry handling, suspension/reactivation workflow) — explicitly undefined in `COMMERCIAL_READINESS.md`; these are owner decisions required before Stage C, not before beta.
- **Staging deployment reality** — `PRODUCTION_OPERATIONS.md` requires a staging environment before release; no staging project, deploy record, or rollback rehearsal exists yet (production deployment NOT VERIFIED).
- **Per-store LINE OA/LIFF onboarding mechanics** — the runbook assumes configured per-shop tokens; how a non-technical store owner obtains/registers their LINE OA and LIFF channel (and whether Pawstia uses one shared OA or store-owned OAs) is not documented as an operational SOP.
- **Whether the visitor camera survives into sell-ready V1** — it is built and secured (Phase 8, 22/22) but has no role in positioning, pricing, or sales scripts; its product status (kept, hidden, or dropped) is an unresolved owner decision.
- **Trademark clearance and legal entity** — preconditions to any public outreach, currently TBD.
- **Portfolio-level gates outside this repo** (PS-A2 Project B admission, P0a-C1) referenced in daily logs — their resolution affects resourcing but not product identity.

## 7. Confidence 0-100

**86**

The product-identity recommendation rests on directly verified, mutually consistent evidence: the PRD contract, the implemented and tested surface (13 migrations, ~250 passing assertions across suites, real-browser E2E, a green full-matrix CI run), the sales/GTM materials, and the honest evidence-culture artifacts. Deductions from 100: (a) ~6 points for inability to independently re-execute the CI run and the pending independent Phase 13 review; (b) ~4 points for confirmed documentation drift that could misstate the very latest state; (c) ~4 points because the demand side (the actual gate question — "for whom") has zero primary evidence, so the buyer/pain claims are internally consistent hypotheses rather than validated facts, and no council output can raise that floor.

