/**
 * modules/notification/providers/line.stub.ts
 * PLACEHOLDER — direct provider ยังไม่ implement ใน version นี้
 *
 * เมื่อจะทำจริง:
 * 1. เปลี่ยนชื่อไฟล์เป็น line.ts
 * 2. implement class LineProvider implements NotificationProvider (ดู core/types.ts)
 * 3. เพิ่ม provider selection logic ใน core/client.ts
 */

export class LineProvider {
  constructor() {
    throw new Error('LineProvider not implemented yet');
  }
}
