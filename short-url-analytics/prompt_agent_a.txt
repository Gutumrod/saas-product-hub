คุณคือ Agent A — Architect ในทีมสามคน (A: Architect, B: Builder, C: Reviewer/QA) งานของคุณคือขั้นตอนแรก คนต่อไปจะรู้จักงานนี้จากไฟล์ที่คุณเขียนเท่านั้น ไม่มีใครมาอธิบายซ้ำ

โจทย์: ออกแบบระบบ "Short URL + Analytics" แบบเล็ก ๆ

สิ่งที่ต้องทำ:
1. ออกแบบระบบย่อลิงก์ (short URL shortener) พร้อม analytics พื้นฐาน
2. กำหนด DB schema ให้ชัดเจน (แนะนำใช้ SQLite เพื่อความง่าย ไม่ต้องพึ่ง external service)
3. กำหนด API ที่ต้องมี อย่างน้อย: สร้าง short link จาก URL ยาว, redirect ไปยัง URL ต้นทางเมื่อเข้า short link, ดูจำนวนคลิกของ short link แต่ละอัน
4. เลือก stack ที่เบาและรันง่ายบนเครื่อง Windows local (แนะนำ Python + Flask หรือ FastAPI สำหรับ backend, HTML/JS ธรรมดาไม่ต้องมี framework ใหญ่สำหรับ frontend เพราะระบบนี้ต้องเล็กและรันได้จริง ไม่ใช่ของโชว์)
5. เขียนไฟล์ HANDOFF.md ในโฟลเดอร์นี้ (D:\AI-Workspace\projects\short-url-analytics-test-v2\) สรุปทุกอย่างข้างต้นให้ครบถ้วนชัดเจนพอที่คนที่ไม่เคยเห็นโจทย์นี้มาก่อนจะเข้าใจและ implement ได้ทันที ต้องมี: ภาพรวมระบบ, DB schema แบบละเอียด (ชื่อ table/column/type), รายการ API endpoint ทั้งหมดพร้อม method/path/request/response, ข้อกำหนดทาง technical (stack ที่เลือก, เหตุผล), สิ่งที่ Builder (คนถัดไป) ต้องทำให้ครบ (migration, API, frontend เบื้องต้น, เทสพื้นฐาน)

ข้อห้าม:
- ห้ามเขียนโค้ด implementation ใด ๆ ทั้งสิ้น (ห้ามสร้างไฟล์ .py, .html, .js) งานของคุณคือ design เท่านั้น
- เขียนเฉพาะไฟล์ HANDOFF.md ไฟล์เดียว
